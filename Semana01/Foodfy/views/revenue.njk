{% extends "layout.njk" %}

{% block title%}
    <title>Revenue</title>
{% endblock %}

<div class="content">

    {% block menu%}
    <nav>
        <div id="menu">
            <img src="./assets/logo.png" alt="Logo" id="logo">
            <a href="./about.html" id="sobre">Sobre</a>
            <a href="./revenue.html" id="receitas">Receitas</a>
        </div>
    </nav>
    {% endblock %}

    {% block content %}
    <section class="info-pizzas">

        {% for item in items %}
        <div class="card" id="{{ item.id }}">
            <div class="card_image">
                <img src="{{ item.image }}" alt="Foto-Burger" class="img">
            </div>
            <div class="card_title">
                <h2>{{ item.title }}</h2>
            </div>
            <div class="card_subtitle">
                <p>{{ item.author}}</p>
            </div>
        </div>
        {% endfor %}

        {% for item in items %}
        <div class="card" id="{{ item.id }}">
            <div class="card_image">
                <img src="{{ item.image }}" alt="Foto-Burger" class="img">
            </div>
            <div class="card_title">
                <h2>{{ item.title }}</h2>
            </div>
            <div class="card_subtitle">
                <p>{{ item.author}}</p>
            </div>
        </div>
        {% endfor %}

    </section>
   

</div>

<script src="/scripts.js"></script>
{% endblock %}